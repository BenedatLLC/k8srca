[project]
name = "k8srca"
version = "0.1.0"
description = "Kubernetes root cause analysis built on Claude Managed Agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "k8stools>=1.1.0",
]
