# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# config
.env
.envrc

# editor files
.*.swp

# other internal docs
/background/

# local state (session map, resolved agent/environment IDs)
/.k8srca/

# per-session sandbox workspaces
/.k8srca/

# local container env (paths + uids, not secret, but host-specific)
/.env.container

# Generated skill bundle: built from THIS deployment's cluster, charts and docs
# by `k8srca arch build`. Cluster topology is not framework content, and would
# make the repo non-portable. The query script it ships lives in
# src/k8srca/arch/templates/ and is copied in at build time.
/skills/cluster-architecture/
